Superficie base: blanco, borde `--color-border` de 1px, radio 8px, sombra apenas perceptible y 24px de padding.

```jsx
<Card title="Pedidos del mes"><div style={{fontSize:28,fontWeight:700}}>1,324</div></Card>
```

Nunca se apila sombra fuerte ni borde de color: el contraste lo da el fondo `--gray-50` de la página.
