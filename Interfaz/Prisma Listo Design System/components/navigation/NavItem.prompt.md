Fila de navegación lateral. Activa = relleno gris de marca + semibold; hover = `--gray-50`.

```jsx
<NavItem icon={<Icon name={20} />} label="Panel" active />
<NavItem label="Pedidos" onClick={go} />
```
