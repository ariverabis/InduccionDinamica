Etiqueta de estado en cápsula (pill), 12px semibold. Sin colores semánticos: Prisma comunica estado con peso y contraste, no con verde/rojo.

```jsx
<Badge>Activo</Badge>
<Badge tone="dark">3 nuevos</Badge>
```

`neutral` = relleno gris de marca; `dark` = fondo blanco con borde negro de 1px.
