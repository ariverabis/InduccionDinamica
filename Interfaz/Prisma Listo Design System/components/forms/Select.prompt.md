Selector nativo con la misma caja que `Input` (10/12px de padding, radio 8px, borde gris).

```jsx
<Select label="País" options={['Venezuela','Colombia','Costa Rica']} />
```

Sin flecha personalizada: se usa la del sistema operativo a propósito.
