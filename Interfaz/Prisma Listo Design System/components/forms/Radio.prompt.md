Opción única, 20px con punto interior de 10px en `--gray-400`.

```jsx
<Radio label="Retiro en tienda" checked={v==='tienda'} onChange={()=>setV('tienda')} />
```

`onChange` no recibe argumento: es un simple "selecciona esta opción".
