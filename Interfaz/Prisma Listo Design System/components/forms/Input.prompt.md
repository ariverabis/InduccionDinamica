Campo de texto con etiqueta arriba (13px semibold) y anillo de foco gris de 2px.

```jsx
<Input label="Nombre del comercio" placeholder="Ferretería El Tornillo" />
```

Estado deshabilitado usa fondo `--gray-50`. El borde pasa de `--color-border-strong` a `--gray-400` al enfocar.
