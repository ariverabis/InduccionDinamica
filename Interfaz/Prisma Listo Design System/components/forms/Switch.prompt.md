Interruptor 40×22 sin etiqueta propia: la etiqueta se compone al lado con un flex de 10px de gap.

```jsx
<div style={{display:'flex',alignItems:'center',gap:10}}><Switch checked={on} onChange={setOn} />Notificaciones</div>
```

Pista gris `--gray-200` apagado / `--gray-400` encendido; el pulgar lleva `--shadow-card`.
